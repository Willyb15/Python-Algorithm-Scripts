#sudo pip install pythonds
from pythonds.basic.stack import Stack
# new() - creates a new stack
# push() adds an object onto the stack
# pop () removes the top item from the stack
# top () returns the top item from the stack without removing it
# size() returns number of objects in the stack
# isEmpty() idicates if the stack is empty
class MyStack:
	def __init__(self):
		self.data = []

	def push(self, item):

    def pop(self):

	def peek(self):

	def size(self):

	def isEmpty(self):
